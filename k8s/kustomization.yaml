apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Argo CD can point at /k8s and sync this app without applying cluster-level manifests.
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pvc.yaml
